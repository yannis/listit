<h1 class="text-3xl">Notifications</h1>
<%= turbo_stream_from(current_user, :notifications) %>
<%= turbo_frame_tag(dom_id(current_user, :notifications)) do %>
  <%= render @notifications %>
  <% if @notifications.empty? %>
    <p>
      No notifications yet
    </p>
  <% end %>
<% end %>
