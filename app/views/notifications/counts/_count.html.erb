<%= turbo_frame_tag(dom_id(current_user, :notifications_count)) do %>
  <%= link_to(unread_notifications_count, notifications_path, target: "_top", class: "mr-1 badge badge-#{unread_notifications_count.positive? ? 'red' : 'green'}") %>
<% end %>
